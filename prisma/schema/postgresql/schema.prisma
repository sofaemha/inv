generator client {
  provider = "prisma-client"
  output   = "../../../src/prisma/postgresql"
}

datasource db {
  provider = "cockroachdb"
}

enum crdb_internal_region {
  aws_ap_southeast_3 @map("aws-ap-southeast-3")
}
